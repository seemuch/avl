int main(){

	bool array[] = {true,true,false,false,false};
	<begin_display>

		test2(array);

	<end_display>
	return 0;
}


bool[] test(bool[] x){
	//if we return an subarray, how to display it?	

	int l = len(x);
	if( l <= 1 )
		return x;
	else
		return x[0:l-1];

}

bool[] test2(bool x[]){

	int l = len(x);
	if( l <= l)
		return x;
	else
		return test(x[0:l-1]);

}
